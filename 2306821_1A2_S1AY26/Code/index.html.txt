<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>EcoWares - Sustainable Products</title>
  <link rel="stylesheet" href="style.css" />
  <style>
 {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Body styles */
body {
  font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
  background-color: #f0f8f5; /* Light eco-friendly background */
  color: #333;
  line-height: 1.6;
}

/* Header styles */
header {
  background-color: #4CAF50; /* Eco green */
  padding: 20px;
  text-align: center;
  color: white;
}

header h1 {
  font-size: 2.5em;
  margin-bottom: 10px;
}

nav a {
  color: white;
  margin: 0 15px;
  text-decoration: none;
  font-weight: 600;
}

nav a:hover {
  text-decoration: underline;
}

/* Main content styles */
.main {
  padding: 40px 20px;
  max-width: 1200px;
  margin: auto;
}

section {
  margin-bottom: 50px;
}

h2 {
  color: #2e7d32; /* Darker green for headings */
  margin-bottom: 20px;
  font-size: 2em;
}

/* Product grid styles */
.products {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
}

/* Individual product card */
.product-card {
  background-color: #ffffff;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 4px 8px rgba(0,0,0,0.1);
  transition: transform 0.3s, box-shadow 0.3s;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 15px;
}

.product-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 16px rgba(0,0,0,0.2);
}

.product-image {
  width: 100%;
  height: auto;
  margin-bottom: 15px;
  border-radius: 4px;
}

.product-title {
  font-size: 1.2em;
  margin-bottom: 10px;
  color: #2e7d32;
  text-align: center;
}

.product-price {
  font-size: 1.1em;
  color: #555;
  margin-bottom: 15px;
}

.btn {
  display: inline-block;
  background-color: #4CAF50;
  color: #fff;
  padding: 12px 25px;
  border-radius: 4px;
  text-decoration: none;
  font-weight: 600;
  transition: background-color 0.3s;
}

.btn:hover {
  background-color: #388e3c;
}

/* Footer styles */
footer {
  background-color: #2e7d32;
  color: white;
  text-align: center;
  padding: 20px;
  margin-top: 50px;
}

footer p {
  margin-bottom: 10px;
}

@media(max-width: 768px) {
  header h1 {
    font-size: 2em;
  }
  h2 {
    font-size: 1.5em;
  }
  .btn {
    padding: 10px 20px;
  }
}
</style>
</head>
<body>
  <header>
    <div class="logo">
      <img src="C:\Users\allen\OneDrive\Pictures\eco-icon-with-green-letters-2-leaves-white-background_95164-12306.jpg" alt="EcoWares Logo" />
      <h1>EcoWares</h1>
    </div>
    <nav>
      <ul class="nav-list">
        <li><a href="index.html">Products</a></li>
        <li><a href="about.html">About Us</a></li>
        <li><a href="cart.html">Cart</a></li>
        <li><a href="login.html">Login</a></li>
        <li><a href="register.html">Register</a></li>
		</ul><a href="login.html">Logout</a>
      </ul>
    </nav>
  </header>
   <main>
        <section class="about-section">
            <h2>About EcoWares</h2>
            <p>
                EcoWares is dedicated to providing sustainable and eco-friendly household products that help reduce plastic waste and promote a greener lifestyle. Our mission is to make eco-conscious living accessible and affordable for everyone.
            </p>
            <h3>Our Team</h3>
            <ul>
                <li><strong>Full Name:</strong> Stephane Allen</li>
                <li><strong>ID#:</strong> 2306821</li>
                <li><strong>Email:</strong> allenstephane3@gmail.com</li>
                <li><strong>Module Code:</strong> CIT2011</li>
                <li><strong>Assignment Name:</strong> IA#2 - E-commerce Website</li>
                <li><strong>Class Schedule:</strong> Thursday @ 6PM</li>
            </ul>
            <h3>Contact Us</h3>
            <p>
                Have questions or comments? Send us an email: 
                <a href="mailto:2306821@utech.edu.jm">2306821@utech.edu.jm</a>
            </p>
        </section>
    </main>

      <section id="products-list">
	<main>
    <h2>Featured Eco-friendly Products</h2>
    <section class="products">
      <article class="product-card" data-id="1">
        <img src="C:\Users\allen\OneDrive\Pictures\web1.jpeg" alt="Reusable Bag" />
        <h3>Reusable Bag</h3>
        <p>Strong, washable, eco-friendly bag for daily shopping.</p>
        <p class="price">$12.99</p>
      </article>

      <article class="product-card" data-id="2">
        <img src="C:\Users\allen\OneDrive\Pictures\kid_brush_1.webp" alt="Bamboo Toothbrush" />
        <h3>Bamboo Toothbrush</h3>
        <p>Biodegradable bamboo toothbrush with soft bristles.</p>
        <p class="price">$5.99</p>
      </article>
	  
	  <article class="product-card" data-id="3">
        <img src="C:\Users\allen\OneDrive\Pictures\images.jpg" alt="Bamboo Straw" />
        <h3>Bamboo Straw</h3>
        <p>Eco-friendly, biodegradable reusable bamboo straw</p>
        <p class="price">$5.99</p>
      </article>
	  
	  <article class="product-card" data-id="4">
        <img src="C:\Users\allen\OneDrive\Pictures\cotton_tote_bag_packshot_800x.webp" alt="Cotton Tote Bag" />
        <h3>Organic Cotton Tote Bag</h3>
        <p>Durable and stylish organic cotton tote bag.</p>
        <p class="price">$12.99</p>
      </article>
	  
	   <article class="product-card" data-id="5">
        <img src="C:\Users\allen\OneDrive\Pictures\pela-case.jpg" alt="Compostable Phone Case" />
        <h3>Compostable Phone Case</h3>
        <p>Made from vegan biodegradable materials.</p>
        <p class="price">$19.99</p>
      </article>
<script>
  // Define the products
  const products = {
    1: { id: 1, name: "Reusable Bag" },
    2: { id: 2, name: "Bamboo Toothbrush" },
    3: { id: 3, name: "Bamboo Straw" },
    4: { id: 4, name: "Compostable Phone Case" },
    5: { id: 5, name: "Organic Cotton Tote Bag" }
  };

  // Initialize cart
  let cart = [];

  // Function to add an item to the cart
  function addToCart(productId) {
    const product = products[productId];
    if (product) {
      cart.push(product);
      updateCartDisplay();
    }
  }

  // Function to clear the cart
  function clearCart() {
    cart = [];
    updateCartDisplay();
  }

  // Function to update the displayed cart
  function updateCartDisplay() {
    const cartList = document.getElementById('cart-list');
    cartList.innerHTML = '';
    if (cart.length === 0) {
      cartList.innerHTML = '<li>Cart is empty.</li>';
    } else {
      cart.forEach(item => {
        const li = document.createElement('li');
        li.textContent = item.name;
        cartList.appendChild(li);
      });
    }
  }

  // Initialize cart display
  updateCartDisplay();
</script>
</body>
</html>
 
 </main>
<div id="cart-page">
  <h2>Your Shopping Cart</h2>
  <table id="cart-table" border="1" cellpadding="10" cellspacing="0">
    <thead>
      <tr>
        <th>Item</th>
        <th>Price</th>
        <th>Quantity</th>
        <th>Sub-total</th>
        <th>Discount</th>
        <th>Tax</th>
        <th>Total</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody id="cart-items">
	<script>
	const cartItems = [
  {
    id: 1,
    name: "Reusable Bag",
    price: 12.99,
    quantity: 2,
    discount: 0, // in percentage
    taxRate: 0.07, // 7%
  },
  {
    id: 2,
    name: "Bamboo Toothbrush",
    price: 5.99,
    quantity: 3,
    discount: 0,
    taxRate: 0.07,
  },
  {
    id: 3,
    name: "Bamboo Straw",
    price: 5.99,
    quantity: 4,
    discount: 0,
    taxRate: 0.07,
  },
  {
    id: 4,
    name: "Organic Cotton Tote Bag",
    price: 12.99,
    quantity: 1,
    discount: 0,
    taxRate: 0.07,
  },
  {
    id: 5,
    name: "Compostable Phone Case",
    price: 19.99,
    quantity: 2,
    discount: 0,
    taxRate: 0.07,
  },
];
</script>
    </tbody>
  </table>
  <button onclick="showCheckout()">Proceed to Checkout</button>
</div>
<script>
function renderCart() {
  const tbody = document.getElementById('cart-items');
  tbody.innerHTML = '';

  let grandTotal = 0;

  cartItems.forEach(item => {
    const subTotal = item.price * item.quantity;
    const discountAmount = (item.discount / 100) * subTotal;
    const discountedTotal = subTotal - discountAmount;
    const tax = discountedTotal * item.taxRate;
    const total = discountedTotal + tax;

    grandTotal += total;

    const row = document.createElement('tr');

    row.innerHTML = `
      <td>${item.name}</td>
      <td>$${item.price.toFixed(2)}</td>
      <td>
        <input type="number" min="1" value="${item.quantity}" onchange="updateQuantity(${item.id}, this.value)">
      </td>
      <td>$${subTotal.toFixed(2)}</td>
      <td>${item.discount}%</td>
      <td>$${tax.toFixed(2)}</td>
      <td>$${total.toFixed(2)}</td>
      <td>
        <button onclick="removeItem(${item.id})">Remove</button>
      </td>
    `;
    tbody.appendChild(row);
  });

  // Display total
  document.getElementById('cart-total').innerText = `Grand Total: $${grandTotal.toFixed(2)}`;
}

function updateQuantity(id, quantity) {
  const item = cartItems.find(i => i.id === id);
  if (item) {
    item.quantity = parseInt(quantity);
    renderCart();
  }
}

function removeItem(id) {
  const index = cartItems.findIndex(i => i.id === id);
  if (index !== -1) {
    cartItems.splice(index, 1);
    renderCart();
  }
}

function clearCart() {
  cartItems.length = 0;
  renderCart();
}

function showCheckout() {
  document.getElementById('cart-page').style.display = 'none';
  document.getElementById('checkout-page').style.display = 'block';
}
</script>
<div id="checkout-page" style="display:none;">
  <h2>Checkout Summary</h2>
  <p id="checkout-total"></p>
  <h3>Shipping Details</h3>
  <form id="shipping-form">
    <label>Name:</label><br>
    <input type="text" id="name" required><br>
    <label>Address:</label><br>
    <textarea id="address" required></textarea><br>
    <label>Amount Being Paid:</label><br>
    <input type="number" id="amount-paid" readonly><br><br>
    <button type="button" onclick="confirmOrder()">Confirm</button>
    <button type="button" onclick="cancelCheckout()">Cancel</button>
  </form>
  <button onclick="clearAll()">Clear All</button>
  <button onclick="finalizeCheckout()">Check Out</button>
  <button onclick="closeCheckout()">Close</button>
</div>
<script>
let totalCost = 0;

function showCheckout() {
  // Calculate total cost again for checkout
  totalCost = 0;
  cartItems.forEach(item => {
    const subTotal = item.price * item.quantity;
    const discountAmount = (item.discount / 100) * subTotal;
    const discountedTotal = subTotal - discountAmount;
    const tax = discountedTotal * item.taxRate;
    const total = discountedTotal + tax;
    totalCost += total;
  });

  document.getElementById('checkout-total').innerText = `Total Cost: $${totalCost.toFixed(2)}`;
  document.getElementById('amount-paid').value = totalCost.toFixed(2);
  document.getElementById('cart-page').style.display = 'none';
  document.getElementById('checkout-page').style.display = 'block';
}

function confirmOrder() {
  alert('Order Confirmed! Thank you for shopping with EcoWares.');
  // You can add further logic for processing payment here
}

function cancelCheckout() {
  document.getElementById('checkout-page').style.display = 'none';
  document.getElementById('cart-page').style.display = 'block';
}

function clearAll() {
  if (confirm('Are you sure you want to clear your cart?')) {
    clearCart();
  }
}

function finalizeCheckout() {
  const name = document.getElementById('name').value;
  const address = document.getElementById('address').value;
  const amountPaid = document.getElementById('amount-paid').value;

  if (name && address && amountPaid) {
    alert(`Thank you, ${name}! Your order totaling $${amountPaid} has been placed.`);
    // Reset cart and form
    cartItems.length = 0;
    renderCart();
    cancelCheckout();
  } else {
    alert('Please fill all shipping details.');
  }
}

function closeCheckout() {
  cancelCheckout();
}
window.onload = () => {
  renderCart();
};
</script>
 </footer>
    <p>© 2025 EcoWares. All rights reserved.</p>
  </footer>
